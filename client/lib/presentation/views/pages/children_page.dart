import 'package:flutter/material.dart';

class ChildrenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Children')),
      body: Center(child: Text('List of Children')),
    );
  }
}
